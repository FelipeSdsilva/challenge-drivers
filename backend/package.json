{
  "name": "backend",
  "version": "1.0.0",
  "description": "This project presents a challenge for Shopper. It aims to resolve a quest about users requesting a trip in a private car from point A to point B.",
  "main": "users can have options for drivers and values, confirm the trip, and list historic of trips.",
  "scripts": {
    "build": "tsc",
    "start": "node  --env-file=.env --import=tsx --watch ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Felipe Sousa",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.9.1",
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
